version: 2
formats: []
build:
  os: ubuntu-24.04
  tools:
    python: '3.10'
python:
  install:
      - requirements: pyproject.toml
      - method: pip
        path: .
        extra-requirements:
          - dev
sphinx:
  configuration: docs/conf.py
  fail_on_warning: true